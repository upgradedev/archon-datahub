name: Bootstrap AWS foundation

run-name: Bootstrap AWS foundation at ${{ inputs.expected_head_sha }}

on:
  workflow_dispatch:
    inputs:
      expected_head_sha:
        description: Exact lowercase 40-character master HEAD approved for this operation
        required: true
        type: string
      confirmation:
        description: Type BOOTSTRAP_CDK_FOUNDATION
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: archon-aws-control-plane
  cancel-in-progress: false
  queue: max

env:
  NODE_VERSION: "22.23.1"
  NPM_VERSION: "10.9.8"
  EXPECTED_REPOSITORY: upgradedev/archon-datahub
  EXPECTED_BRANCH: master
  EXPECTED_ENVIRONMENT: aws-foundation
  FOUNDATION_ROLE_NAME: archon-datahub-github-foundation
  EXPECTED_BOOTSTRAP_VERSION: "32"
  AWS_PAGER: ""

defaults:
  run:
    shell: bash

jobs:
  foundation:
    name: Reconcile and attest AWS foundation
    concurrency:
      group: archon-governed-canary-mutation-recovery
      cancel-in-progress: false
      queue: max
    runs-on: ubuntu-24.04
    timeout-minutes: 55
    environment:
      name: aws-foundation
    permissions:
      actions: read
      attestations: write
      contents: read
      deployments: read
      id-token: write
    steps:
      - name: Validate protected master control plane
        id: source
        env:
          GH_TOKEN: ${{ github.token }}
          EXPECTED_HEAD_SHA_INPUT: ${{ inputs.expected_head_sha }}
          CONFIRMATION_INPUT: ${{ inputs.confirmation }}
          CONTROL_PLANE_SHA: ${{ github.sha }}
          WORKFLOW_SHA: ${{ github.workflow_sha }}
          WORKFLOW_REF: ${{ github.workflow_ref }}
          AWS_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}
          AWS_REGION: ${{ vars.AWS_REGION }}
          AWS_FOUNDATION_ROLE_ARN: ${{ vars.AWS_FOUNDATION_ROLE_ARN }}
          AWS_SHARED_API_GATEWAY_ROLE_ARN_SHA256: ${{ vars.AWS_SHARED_API_GATEWAY_ROLE_ARN_SHA256 }}
          STAGING_CLOUDFRONT_DOMAIN_NAME: ${{ vars.STAGING_CLOUDFRONT_DOMAIN_NAME }}
          STAGING_CLOUDFRONT_HOSTED_ZONE_ID: ${{ vars.STAGING_CLOUDFRONT_HOSTED_ZONE_ID }}
          PRODUCTION_CLOUDFRONT_DOMAIN_NAME: ${{ vars.PRODUCTION_CLOUDFRONT_DOMAIN_NAME }}
          PRODUCTION_CLOUDFRONT_HOSTED_ZONE_ID: ${{ vars.PRODUCTION_CLOUDFRONT_HOSTED_ZONE_ID }}
        run: |
          set -euo pipefail
          : "${AWS_ACCOUNT_ID:?aws-foundation AWS_ACCOUNT_ID is required}"
          : "${AWS_REGION:?aws-foundation AWS_REGION is required}"
          : "${AWS_FOUNDATION_ROLE_ARN:?aws-foundation AWS_FOUNDATION_ROLE_ARN is required}"
          : "${STAGING_CLOUDFRONT_DOMAIN_NAME:?aws-foundation STAGING_CLOUDFRONT_DOMAIN_NAME is required}"
          : "${STAGING_CLOUDFRONT_HOSTED_ZONE_ID:?aws-foundation STAGING_CLOUDFRONT_HOSTED_ZONE_ID is required}"
          : "${PRODUCTION_CLOUDFRONT_DOMAIN_NAME:?aws-foundation PRODUCTION_CLOUDFRONT_DOMAIN_NAME is required}"
          : "${PRODUCTION_CLOUDFRONT_HOSTED_ZONE_ID:?aws-foundation PRODUCTION_CLOUDFRONT_HOSTED_ZONE_ID is required}"
          echo "::add-mask::${AWS_ACCOUNT_ID}"
          echo "::add-mask::${AWS_FOUNDATION_ROLE_ARN}"

          test "${GITHUB_REPOSITORY}" = "${EXPECTED_REPOSITORY}"
          test "${GITHUB_ACTOR}" = "${GITHUB_REPOSITORY_OWNER}"
          test "${GITHUB_TRIGGERING_ACTOR}" = "${GITHUB_REPOSITORY_OWNER}"
          test "${GITHUB_REF}" = "refs/heads/${EXPECTED_BRANCH}"
          test "${GITHUB_REF_NAME}" = "${EXPECTED_BRANCH}"
          test "${CONTROL_PLANE_SHA}" = "${WORKFLOW_SHA}"
          test "${WORKFLOW_REF}" = \
            "${EXPECTED_REPOSITORY}/.github/workflows/aws-foundation.yml@refs/heads/${EXPECTED_BRANCH}"
          test "${CONFIRMATION_INPUT}" = "BOOTSTRAP_CDK_FOUNDATION"
          [[ "${EXPECTED_HEAD_SHA_INPUT}" =~ ^[0-9a-f]{40}$ ]]
          test "${EXPECTED_HEAD_SHA_INPUT}" = "${CONTROL_PLANE_SHA}"
          [[ "${AWS_ACCOUNT_ID}" =~ ^[0-9]{12}$ ]]
          test "${AWS_REGION}" = "eu-west-1"
          test "${AWS_FOUNDATION_ROLE_ARN}" = \
            "arn:aws:iam::${AWS_ACCOUNT_ID}:role/${FOUNDATION_ROLE_NAME}"
          if [[ -n "${AWS_SHARED_API_GATEWAY_ROLE_ARN_SHA256}" ]]; then
            [[ "${AWS_SHARED_API_GATEWAY_ROLE_ARN_SHA256}" =~ ^[0-9a-f]{64}$ ]]
          fi
          dns_name_pattern='^([a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?\.)+[a-z]([a-z0-9-]{0,61}[a-z0-9])?$'
          hosted_zone_id_pattern='^Z[A-Z0-9]{1,31}$'
          [[ "${STAGING_CLOUDFRONT_DOMAIN_NAME}" =~ ${dns_name_pattern} ]]
          [[ "${PRODUCTION_CLOUDFRONT_DOMAIN_NAME}" =~ ${dns_name_pattern} ]]
          [[ "${STAGING_CLOUDFRONT_HOSTED_ZONE_ID}" =~ ${hosted_zone_id_pattern} ]]
          [[ "${PRODUCTION_CLOUDFRONT_HOSTED_ZONE_ID}" =~ ${hosted_zone_id_pattern} ]]
          test "${STAGING_CLOUDFRONT_DOMAIN_NAME}" != \
            "${PRODUCTION_CLOUDFRONT_DOMAIN_NAME}"

          repository_json="$(
            gh api \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2026-03-10" \
              "/repos/${GITHUB_REPOSITORY}"
          )"
          test "$(jq -er '.default_branch' <<<"${repository_json}")" = \
            "${EXPECTED_BRANCH}"
          remote_ref="$(
            gh api \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2026-03-10" \
              "/repos/${GITHUB_REPOSITORY}/git/ref/heads/${EXPECTED_BRANCH}"
          )"
          test "$(
            jq -er \
              --arg ref "refs/heads/${EXPECTED_BRANCH}" '
                select(
                  .ref == $ref and
                  .object.type == "commit" and
                  (.object.sha | test("^[0-9a-f]{40}$"))
                ) |
                .object.sha
              ' <<<"${remote_ref}"
          )" = "${CONTROL_PLANE_SHA}"

          environment_json="$(
            gh api \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2026-03-10" \
              "/repos/${GITHUB_REPOSITORY}/environments/${EXPECTED_ENVIRONMENT}"
          )"
          jq -e \
            --arg environment "${EXPECTED_ENVIRONMENT}" \
            --arg reviewer "${GITHUB_REPOSITORY_OWNER}" '
              .name == $environment and
              .can_admins_bypass == false and
              .deployment_branch_policy.protected_branches == false and
              .deployment_branch_policy.custom_branch_policies == true and
              (
                [.protection_rules[]?.type] | sort
              ) == ["branch_policy", "required_reviewers"] and
              (
                [.protection_rules[]? |
                  select(.type == "required_reviewers")][0] as $rule |
                $rule.prevent_self_review == false and
                ($rule.reviewers | length) == 1 and
                $rule.reviewers[0].type == "User" and
                $rule.reviewers[0].reviewer.login == $reviewer
              )
            ' <<<"${environment_json}" >/dev/null
          branch_policies="$(
            gh api \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2026-03-10" \
              "/repos/${GITHUB_REPOSITORY}/environments/${EXPECTED_ENVIRONMENT}/deployment-branch-policies?per_page=100"
          )"
          jq -e --arg branch "${EXPECTED_BRANCH}" '
            .total_count == 1 and
            (.branch_policies | length) == 1 and
            .branch_policies[0].name == $branch and
            .branch_policies[0].type == "branch"
          ' <<<"${branch_policies}" >/dev/null

      - name: Check out exact master control plane
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Verify foundation source contracts
        env:
          CONTROL_PLANE_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${CONTROL_PLANE_SHA}"
          test -z "$(git status --porcelain --untracked-files=all)"
          for path in \
            contracts/aws-foundation-v1.json \
            contracts/aws-foundation-policy-migration-v1.json \
            contracts/aws-foundation-core-ami-policy-migration-v1.json \
            contracts/aws-foundation-cloud-runtime-publisher-policy-migration-v1.json \
            .github/workflows/aws-foundation-cloud-runtime-publisher-policy-migration.yml \
            .github/workflows/aws-foundation-cloud-runtime-publisher-policy-migration-driver.yml \
            .github/workflows/aws-foundation-cloud-runtime-publisher-policy-migration-cleanup.yml \
            .github/workflows/datahub-cloud-runtime-image.yml \
            .github/workflows/aws-foundation-core-ami-policy-migration.yml \
            .github/workflows/aws-foundation-core-ami-policy-migration-driver.yml \
            .github/workflows/aws-foundation-core-ami-policy-migration-cleanup.yml \
            infra/aws/package.json \
            infra/aws/package-lock.json \
            infra/aws/foundation/api-gateway-account.yml \
            infra/aws/foundation/cdk-execution-policy.yml \
            infra/aws/foundation/github-actions-deploy-role.yml \
            infra/aws/foundation/github-actions-foundation-policy.json \
            infra/aws/foundation/github-actions-foundation-role.yml \
            infra/aws/foundation/governed-canary-roles.yml \
            infra/aws/foundation/datahub-core-ami-builder.yml \
            infra/aws/packer/datahub-core-ami-builder-policy-contract.json \
            scripts/bootstrap-aws-foundation-role.sh \
            scripts/aws-cloudformation-drift.sh \
            scripts/patch-cdk-brace-expansion.sh \
            scripts/patch-cdk-bootstrap-template.mjs \
            scripts/render-canonical-flow-yaml.mjs \
            scripts/render-inline-cloudformation-template.sh \
            scripts/seal-cdk-bootstrap-templates.sh \
            scripts/reconcile-aws-foundation.sh \
            scripts/reconcile-datahub-core-ami-foundation.sh \
            scripts/aws-foundation-cloud-runtime-publisher-policy-migration-common.sh \
            scripts/aws-foundation-cloud-runtime-publisher-policy-migration-authorization.sh \
            scripts/aws-foundation-cloud-runtime-publisher-policy-migration-state.sh \
            scripts/run-aws-foundation-cloud-runtime-publisher-policy-migration.sh \
            scripts/aws-foundation-core-ami-policy-migration-common.sh \
            scripts/aws-foundation-core-ami-policy-migration-authorization.sh \
            scripts/aws-foundation-core-ami-policy-migration-state.sh \
            scripts/run-aws-foundation-core-ami-policy-migration.sh \
            scripts/sanitize-cloudformation-failure.mjs \
            scripts/render-aws-foundation-policy.mjs \
            scripts/validate-aws-foundation-policy.jq \
            scripts/verify-aws-runtime-boundary.mjs \
            scripts/verify-cdk-npm-audit-compensation.sh \
            scripts/verify-exact-npm-overrides.mjs; do
            test -f "${path}"
            test ! -L "${path}"
          done
          jq -e '
            .schemaVersion == "archon.aws-foundation/v1" and
            .repository == "upgradedev/archon-datahub" and
            .defaultBranch == "master" and
            .workflow.path == ".github/workflows/aws-foundation.yml" and
            .workflow.environment == "aws-foundation" and
            .workflow.confirmation == "BOOTSTRAP_CDK_FOUNDATION" and
            .aws.primaryRegion == "eu-west-1" and
            .aws.regions == ["eu-west-1", "us-east-1"] and
            .aws.toolchain == {
              awsCdkCliVersionSource:
                "infra/aws/package-lock.json#packages/node_modules/aws-cdk/version",
              installedAwsCdkCliMatchesLock: true,
              nodeVersion: "22.23.1",
              npmVersion: "10.9.8"
            } and
            .aws.foundationRoleName == "archon-datahub-github-foundation" and
            .aws.foundationRoleAdoption == {
              allowsOnlyLegacyOrCanonicalComponentsForInterruptedRetry: true,
              convergesToCanonicalRole: true,
              knownLegacyDescription:
                "GitHub OIDC role for the Archon DataHub CDK foundation bootstrap pipeline",
              knownLegacyTags: {
                Application: "archon-datahub",
                Environment: "foundation",
                ManagedBy: "github-actions"
              },
              knownLegacyTrustDifference: "missing-canonical-sid-only",
              requiresZeroAttachedManagedPolicies: true,
              requiresZeroInlinePolicies: true
            } and
            .aws.foundationPolicies.migrationContract ==
              "contracts/aws-foundation-policy-migration-v1.json" and
            .aws.foundationPolicies.identityRoleMigration == {
              baselineCanonicalSha256:
                "afda76cf8cfddd34c876147a4b228dd51b63edc4fd810f6793eb22d462beb553",
              baselineDefaultVersion: "v1",
              contract:
                "contracts/aws-foundation-cloud-runtime-publisher-policy-migration-v1.json",
              exactRoleDelta: [
                "archon-datahub-github-staging-cloud-trial",
                "archon-datahub-github-production-cloud-trial",
                "archon-datahub-cloud-runtime-publish-production"
              ],
              policyName: "archon-aws-foundation-identity",
              targetCanonicalSha256:
                "f8aab593f428ac9d990cefb525d0919241e81c42b09f22d737a97d1fd3dc18a3",
              targetVersion: "v2",
              workflow:
                ".github/workflows/aws-foundation-cloud-runtime-publisher-policy-migration.yml"
            } and
            .aws.foundationPolicies.sourceBundleAttachable == false and
            .aws.foundationPolicies.roleInlinePolicyCount == 0 and
            .aws.foundationPolicies.roleAttachedPolicyCount == 4 and
            .aws.foundationPolicies.roleManagedPolicyQuota == 10 and
            .aws.foundationPolicies.roleManagedPolicyHeadroom == 6 and
            .aws.foundationPolicies.maximumDocumentBytes == 6144 and
            .aws.foundationPolicies.attachedPolicyNames == [
              "archon-aws-foundation-control",
              "archon-aws-foundation-assets",
              "archon-aws-foundation-identity",
              "archon-aws-foundation-attachments"
            ] and
            .aws.inlineTemplateRendering == {
              deployedOriginalMatchesSemanticSha256: true,
              flowEmitter: {
                name: "scripts/render-canonical-flow-yaml.mjs",
                runtime: "node-22.23.1",
                scalarPolicy: "strict-ascii-plain-or-json-quoted"
              },
              format: "canonical-flow-yaml",
              iamPolicyResourceArnValidation: {
                directStrings: true,
                intrinsicSubStrings: true,
                malformedOrIncompleteArns: "forbidden",
                partitionResolution: "literal-aws-or-exact-AWS::Partition-ref",
                serviceSegment: "literal-lowercase-token",
                verifier: "scripts/verify-iam-policy-resource-arns.mjs",
                wildcardServiceSegments: "forbidden"
              },
              maximumTemplateBodyBytes: 51200,
              outputRoot: "RUNNER_TEMP",
              renderer: "scripts/render-inline-cloudformation-template.sh",
              sameBytesForValidationAndDeploy: true,
              semanticRoundTrip: "rain-json-canonical-equality",
              source: "infra/aws/foundation/cdk-execution-policy.yml",
              tool: {
                linuxAmd64ArchiveSha256:
                  "5358d6daf35322101566376a38e37d1f89c6588479af2e20240579fc2d4c660a",
                name: "aws-cloudformation/rain",
                version: "v1.24.4"
              },
              yamlParser: {
                linuxAmd64Sha256:
                  "1bb99e1019e23de33c7e6afc23e93dad72aad6cf2cb03c797f068ea79814ddb0",
                name: "mikefarah/yq",
                version: "v4.47.2"
              }
            } and
            .aws.cloudRuntimePublisher.environment == "production" and
            .aws.cloudRuntimePublisher.condition == "IsProduction" and
            .aws.cloudRuntimePublisher.stackName ==
              "Archon-GitHub-Production-Deploy-Role" and
            .aws.cloudRuntimePublisher.roleName ==
              "archon-datahub-cloud-runtime-publish-production" and
            .aws.cloudRuntimePublisher.roleVariable ==
              "AWS_CLOUD_RUNTIME_IMAGE_ROLE_ARN" and
            .aws.cloudRuntimePublisher.foundationWorkflowOutput ==
              "steps.reconcile.outputs.cloud_runtime_publisher_role_arn" and
            .aws.cloudRuntimePublisher.stackOutputs == [
              "CloudRuntimeImagePublisherRoleArn",
              "CloudRuntimeImagePublisherRoleName"
            ] and
            .aws.cloudRuntimePublisher.workflow == {
              branch: "master",
              name: "DataHub Cloud runtime OCI v2",
              path: ".github/workflows/datahub-cloud-runtime-image.yml",
              preTrustWorkflowRef:
                "upgradedev/archon-datahub/.github/workflows/datahub-cloud-runtime-image.yml@refs/heads/master"
            } and
            .aws.cloudRuntimePublisher.oidcClaims == {
              aud: "sts.amazonaws.com",
              environment: "production",
              ref: "refs/heads/master",
              repository: "upgradedev/archon-datahub",
              sub:
                "repo:upgradedev/archon-datahub:environment:production",
              workflow: "DataHub Cloud runtime OCI v2",
              workflowRefInIam: false
            } and
            .aws.cloudRuntimePublisher.maximumSessionDurationSeconds == 3600 and
            .aws.cloudRuntimePublisher.workflowRequestedSessionSeconds == 1800 and
            .aws.cloudRuntimePublisher.repository ==
              "archon-datahub-cloud-runtime-v2" and
            .aws.cloudRuntimePublisher.region == "eu-west-1" and
            .aws.cloudRuntimePublisher.inlinePolicyName ==
              "archon-datahub-cloud-runtime-ecr-publish" and
            .aws.cloudRuntimePublisher.requiredRepositoryTags == {
              "archon:component": "datahub-cloud-runtime-v2",
              "archon:owner": "github-actions"
            } and
            (.aws.cloudRuntimePublisher.forbiddenActions | sort) ==
              (["ecr:DeleteRepository", "ecr:PutLifecyclePolicy",
                "ecr:SetRepositoryPolicy", "iam:*", "kms:*"] | sort) and
            .aws.coreAmiFoundation.stackName ==
              "Archon-DataHub-Core-AMI-Builder-Foundation" and
            .aws.coreAmiFoundation.template ==
              "infra/aws/foundation/datahub-core-ami-builder.yml" and
            .aws.coreAmiFoundation.region == "eu-west-1" and
            .aws.coreAmiFoundation.terminationProtection == true and
            .aws.coreAmiFoundation.idleAwsCost == "zero" and
            .aws.coreAmiFoundation.reconciler ==
              "scripts/reconcile-datahub-core-ami-foundation.sh" and
            .aws.coreAmiFoundation.evidenceSchemaVersion ==
              "archon.aws-core-ami-foundation-evidence/v1" and
            .aws.coreAmiFoundation.policyMigration.contract ==
              "contracts/aws-foundation-core-ami-policy-migration-v1.json" and
            .aws.coreAmiFoundation.policyMigration.workflow ==
              ".github/workflows/aws-foundation-core-ami-policy-migration.yml" and
            .aws.coreAmiFoundation.policyMigration.policyName ==
              "archon-aws-foundation-control" and
            .aws.coreAmiFoundation.policyMigration.baselineDefaultVersion ==
              "v2" and
            .aws.coreAmiFoundation.policyMigration.targetVersionStrategy ==
              "aws-assigned-monotonic" and
            .aws.coreAmiFoundation.policyMigration.minimumTargetVersionOrdinal ==
              3 and
            .aws.coreAmiFoundation.policyMigration.deltaSids == [
              "ReconcileExactCoreAmiFoundationStack",
              "ReconcileExactCoreAmiFoundationRoles",
              "ReconcileExactCoreAmiBuilderProfile",
              "AttachExactCoreAmiBuilderSsmPolicy",
              "PassExactCoreAmiBuilderRoleForProfile"
            ] and
            .aws.coreAmiFoundation.policyMigration.replacementSids == [
              "InspectExistingApplicationStackRoles"
            ] and
            .aws.coreAmiFoundation.githubBuildRole.roleName ==
              "archon-datahub-core-ami-build-staging" and
            .aws.coreAmiFoundation.githubBuildRole.variable ==
              "AWS_CORE_AMI_BUILD_ROLE_ARN" and
            .aws.coreAmiFoundation.githubBuildRole.stackOutput ==
              "GitHubCoreAmiBuildRoleArn" and
            .aws.coreAmiFoundation.githubBuildRole.foundationWorkflowOutput ==
              "steps.core_ami_foundation.outputs.build_role_arn" and
            .aws.coreAmiFoundation.githubBuildRole.environment == "staging" and
            .aws.coreAmiFoundation.githubBuildRole.maxSessionDurationSeconds ==
              7200 and
            .aws.coreAmiFoundation.githubBuildRole.workflowRequestedSessionSeconds ==
              7200 and
            .aws.coreAmiFoundation.githubBuildRole.policyContract ==
              "infra/aws/packer/datahub-core-ami-builder-policy-contract.json" and
            .aws.coreAmiFoundation.githubBuildRole.packerAmazonPluginVersion ==
              "1.8.0" and
            .aws.coreAmiFoundation.githubBuildRole.inlinePolicyCharacterLimit ==
              10240 and
            .aws.coreAmiFoundation.githubBuildRole.inlinePolicySizeValidation ==
              "compact-live-policy-document-before-evidence" and
            .aws.coreAmiFoundation.githubBuildRole.requestTagObservation == {
              accountBinding:
                "validate-active-caller-then-omit-before-digest",
              action: "cloudtrail:LookupEvents",
              buildIdPattern: "^[1-9][0-9]{0,19}$",
              evidenceSchemaVersion:
                "archon.packer-request-tag-proof/v1",
              events: [
                "CreateKeyPair",
                "CreateSecurityGroup",
                "RunInstances"
              ],
              principalBinding:
                "exact-oidc-role-session-and-build-window",
              region: "eu-west-1",
              requiredTags: {
                Application: "archon-datahub",
                Environment: "staging",
                ManagedBy: "github-actions",
                "archon:BuildRun": "exact-buildId",
                "archon:Purpose": "datahub-core-ami"
              },
              resource: "*",
              resourceScopeReason:
                "aws-action-does-not-support-resource-level-permissions",
              retainedEvidence:
                "sanitized-counts-and-canonical-digests-only",
              sanitizedProjection: [
                "eventName",
                "eventTime",
                "awsRegion",
                "roleSessionName",
                "requestTagSpecifications"
              ]
            } and
            .aws.coreAmiFoundation.githubBuildRole.inlinePolicyName ==
              "archon-datahub-core-ami-build" and
            .aws.coreAmiFoundation.githubBuildRole.tags == {
              Application: "archon-datahub",
              Environment: "staging",
              ManagedBy: "aws-foundation",
              "archon:Purpose": "datahub-core-ami-build"
            } and
            .aws.coreAmiFoundation.githubBuildRole.trust == {
              audience: "sts.amazonaws.com",
              environment: "staging",
              ref: "refs/heads/master",
              repository: "upgradedev/archon-datahub",
              subject:
                "repo:upgradedev/archon-datahub:environment:staging",
              workflowRef:
                "upgradedev/archon-datahub/.github/workflows/datahub-core-ami.yml@refs/heads/master"
            } and
            .aws.coreAmiFoundation.ec2Builder.roleName ==
              "archon-datahub-core-ami-builder-staging" and
            .aws.coreAmiFoundation.ec2Builder.instanceProfileName ==
              "archon-datahub-core-ami-builder-staging" and
            .aws.coreAmiFoundation.ec2Builder.instanceProfileVariable ==
              "AWS_CORE_AMI_BUILDER_INSTANCE_PROFILE" and
            .aws.coreAmiFoundation.ec2Builder.stackOutputs == [
              "CoreAmiBuilderRoleArn",
              "CoreAmiBuilderInstanceProfileName",
              "CoreAmiBuilderInstanceProfileArn"
            ] and
            .aws.coreAmiFoundation.ec2Builder.instanceProfileFoundationWorkflowOutput ==
              "steps.core_ami_foundation.outputs.instance_profile" and
            .aws.coreAmiFoundation.ec2Builder.maxSessionDurationSeconds ==
              3600 and
            .aws.coreAmiFoundation.ec2Builder.attachedManagedPolicies == [
              "arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore"
            ] and
            .aws.coreAmiFoundation.ec2Builder.inlinePolicyCount == 0 and
            .aws.coreAmiFoundation.ec2Builder.tags == {
              Application: "archon-datahub",
              Environment: "staging",
              ManagedBy: "aws-foundation",
              "archon:Purpose": "datahub-core-ami-builder"
            } and
            .aws.coreAmiFoundation.ec2Builder.profileTagging == {
              cloudFormationNative: false,
              extraOrMismatchedTags: "fail-closed",
              method:
                "foundation-reconciler-exact-iam-tag-instance-profile"
            } and
            .aws.coreAmiFoundation.baseImage.ownerAccount == "137112412989" and
            .aws.coreAmiFoundation.baseImage.ssmParameter ==
              "/aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-x86_64" and
            .aws.coreAmiFoundation.ephemeralBoundary.region == "eu-west-1" and
            .aws.coreAmiFoundation.ephemeralBoundary.publicIngressAllowed ==
              false and
            .aws.coreAmiFoundation.ephemeralBoundary.workflowIamMutationAllowed ==
              false and
            .aws.coreAmiFoundation.githubEnvironment == {
              name: "staging",
              requiredVariables: [
                "AWS_ACCOUNT_ID",
                "AWS_REGION",
                "AWS_CORE_AMI_BUILD_ROLE_ARN",
                "AWS_CORE_AMI_BUILDER_INSTANCE_PROFILE"
              ]
            } and
            .aws.sharedApiGatewayLogging == {
              external: {
                bindingSha256Variable:
                  "AWS_SHARED_API_GATEWAY_ROLE_ARN_SHA256",
                managedStackMustBeAbsent: true,
                mutation: "none",
                sameAccountRoleArnRequired: true,
                takeover: "forbidden"
              },
              managed: {
                inlinePolicyName: "archon-apigateway-cloudwatch-logs",
                roleName: "archon-datahub-apigateway-cloudwatch-logs",
                stackName: "Archon-Shared-ApiGateway-Logging"
              },
              ownershipModes: ["foundation-managed", "external-pinned"],
              region: "eu-west-1"
            } and
            .aws.deployRoles.staging.roleName ==
              "archon-datahub-github-staging-deploy" and
            .aws.deployRoles.production.roleName ==
              "archon-datahub-github-production-deploy" and
            .aws.deployRoles.dataHubCloudTrial.foundationRole == {
              stackNameTemplate: "Archon-GitHub-<stage>-Deploy-Role",
              stackOutputs: [
                "GitHubDataHubCloudTrialRoleArn",
                "GitHubDataHubCloudTrialRoleName"
              ],
              workflowOutputs: {
                production:
                  "steps.reconcile.outputs.datahub_cloud_trial_production_role_arn",
                staging:
                  "steps.reconcile.outputs.datahub_cloud_trial_staging_role_arn"
              }
            } and
            .aws.operationalRoles == {
              judgeAccess: {
                production: {
                  environment: "judge-access-production",
                  roleName: "archon-production-judge-user",
                  variable: "AWS_JUDGE_USER_ROLE_ARN"
                },
                staging: {
                  environment: "judge-access-staging",
                  roleName: "archon-staging-judge-user",
                  variable: "AWS_JUDGE_USER_ROLE_ARN"
                }
              },
              managedPolicyCountPerRole: 0,
              maximumSessionDurationSeconds: 3600,
              productionObserver: {
                environment: "production-observer",
                roleName: "archon-production-posture-observer",
                variable: "AWS_READ_ROLE_ARN"
              },
              productionPagingTest: {
                environment: "production-paging-test",
                roleName: "archon-production-paging-test",
                variable: "AWS_PAGING_TEST_ROLE_ARN"
              },
              productionRuntimeRead: {
                environment: "production-observer",
                roleName: "archon-production-runtime-read",
                variable: "AWS_RUNTIME_READ_ROLE_ARN"
              }
            } and
            .aws.publicViewerDns == {
              acmActions: [
                "acm:AddTagsToCertificate",
                "acm:DeleteCertificate",
                "acm:DescribeCertificate",
                "acm:ListTagsForCertificate",
                "acm:RemoveTagsFromCertificate",
                "acm:RequestCertificate",
                "acm:UpdateCertificateOptions"
              ],
              aliasRecordTypes: ["A", "AAAA"],
              certificateExport: "DISABLED",
              certificateKeyAlgorithm: "EC_prime256v1",
              certificateRegion: "us-east-1",
              certificateTransparencyLogging: "ENABLED",
              certificateValidation: "DNS",
              cloudFrontMinimumProtocolVersion: "TLSv1.3_2025",
              deploymentVariables: {
                domainName: "ARCHON_CLOUDFRONT_DOMAIN_NAME",
                hostedZoneId: "ARCHON_CLOUDFRONT_HOSTED_ZONE_ID"
              },
              executionPolicyFamilies: {
                aliasesAndCanonicalHost: "delivery",
                certificateAndValidation: "edge"
              },
              foundationVariables: {
                productionDomainName:
                  "PRODUCTION_CLOUDFRONT_DOMAIN_NAME",
                productionHostedZoneId:
                  "PRODUCTION_CLOUDFRONT_HOSTED_ZONE_ID",
                stagingDomainName: "STAGING_CLOUDFRONT_DOMAIN_NAME",
                stagingHostedZoneId:
                  "STAGING_CLOUDFRONT_HOSTED_ZONE_ID"
              },
              requiresDistinctStageHostnames: true,
              route53Actions: [
                "route53:ChangeResourceRecordSets",
                "route53:GetChange",
                "route53:GetHostedZone",
                "route53:ListResourceRecordSets"
              ],
              route53DomainsAllowed: false,
              validationRecordTypes: ["CNAME"]
            } and
            .aws.governedCanaryRoles.stackName ==
              "Archon-Governed-Canary-Roles" and
            (
              .aws.governedCanaryRoles.roles |
              keys |
              sort
            ) == ["approval", "prepare", "recovery"] and
            .aws.privateLink.route53Actions ==
              ["route53:AssociateVPCWithHostedZone"] and
            .aws.privateLink.route53VpcCondition ==
              "VPCId=vpc-*,VPCRegion=eu-west-1" and
            .aws.privateLink.route53VpcArnConditionAllowed == false and
            .aws.privateLink.disassociateVpcFromHostedZoneAllowed == false and
            .aws.stages.staging.qualifier == "archonstg" and
            .aws.stages.production.qualifier == "archonprd" and
            (.aws.stages.staging.executionPolicyNames | length) == 10 and
            (.aws.stages.production.executionPolicyNames | length) == 10 and
            (
              .aws.stages.staging.executionPolicyAttachments["eu-west-1"] |
              length
            ) == 9 and
            (
              .aws.stages.staging.executionPolicyAttachments["us-east-1"] |
              length
            ) == 2 and
            .aws.stages.staging.executionPolicyAttachments ==
              .aws.stages.production.executionPolicyAttachments and
            .aws.bootstrap.pinnedVersion == 32 and
            .aws.bootstrap.terminationProtection == true and
            .aws.runtimeBoundary.schemaVersion ==
              "archon.aws-runtime-boundary/v1" and
            .oidc.operationalSubjects == [
              "repo:upgradedev/archon-datahub:environment:judge-access-production",
              "repo:upgradedev/archon-datahub:environment:judge-access-staging",
              "repo:upgradedev/archon-datahub:environment:production-observer",
              "repo:upgradedev/archon-datahub:environment:production-paging-test"
            ] and
            (.aws.runtimeBoundary.allowedActions | length) > 0 and
            (
              .aws.runtimeBoundary.approvedAwsManagedPolicies |
              keys |
              sort
            ) == [
              "arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole"
            ] and
            .evidence.applicationStackRolePreflight == {
              allowedEntryValidations: [
                "passed",
                "requires-explicit-deploy-migration"
              ],
              deployFinalPostcheckRequiresExactBindings: true,
              entryCount: 6,
              migrationRequiredState:
                "foundation-complete-deploy-migration-required",
              perStageDeployEntryCount: 3,
              readyState: "ready-for-deploy"
            } and
            .evidence.driftVerification == {
              cliConnectTimeoutSeconds: 5,
              cliReadTimeoutSeconds: 15,
              awsSdkMaxAttempts: 1,
              coverage: "cloudformation-supported-resources",
              exactStackIncarnation: true,
              failureMode: "fail-closed",
              globalDeadline: "hard-wall-clock",
              globalTimeoutSeconds: 900,
              helper: "scripts/aws-cloudformation-drift.sh",
              maximumConsecutiveApiFailuresPerStack: 3,
              maximumPollAttemptsPerStack: 120,
              method: "detect-then-bounded-describe-poll",
              pollDelaySeconds: 2,
              rawAwsStderr: "suppressed",
              responseBinding: [
                "StackDriftDetectionId",
                "StackId",
                "Timestamp",
                "DriftInformation.LastCheckTimestamp"
              ],
              finalStackBinding: {
                method: "bounded-describe-stacks-poll",
                maximumAttempts: 5,
                pollDelaySeconds: 2,
                timestampComparison: "normalized-utc-monotonic-lower-bound",
                currentOrNewerInSyncProjection: "accept",
                currentDriftedProjection: "fail-closed",
                currentOrNewerIndeterminateProjection: "bounded-retry",
                staleOrUnpublishedProjection: "bounded-retry",
                selectionMismatch: "fail-closed",
                malformedProjection: "fail-closed"
              },
              staleResourceEvidence: "forbidden",
              terminalSuccess: {
                detectionStatus: "DETECTION_COMPLETE",
                driftedStackResourceCount: 0,
                returnedResourceStatuses: ["IN_SYNC"],
                stackDriftStatus: "IN_SYNC"
              },
              unsupportedCliWaiter: "forbidden"
            } and
            .evidence.failureDiagnostics == {
              allowlistedStackLabels: [
                "governed-canary-roles",
                "production-bootstrap-edge",
                "production-bootstrap-primary",
                "production-deploy",
                "production-iam",
                "shared-api-gateway",
                "staging-bootstrap-edge",
                "staging-bootstrap-primary",
                "staging-deploy",
                "staging-iam"
              ],
              artifactFiles: ["SHA256SUMS", "cfn-failure.json"],
              artifactRetentionDays: 90,
              artifactUpload: "explicit-files-only",
              autoRecovery: "forbidden",
              explicitIncidentRecoveryContract:
                "contracts/aws-incident-recovery-v1.json",
              explicitIncidentRecoveryStatus:
                "recovered-delete-complete-cleanup-proven",
              canonicalJson: true,
              credentialClearProofRequired: true,
              diagnosticCount: 1,
              digestFields: [
                "logicalResourceId",
                "reasonCategory",
                "resourceStatus",
                "resourceType",
                "schemaVersion",
                "stackLabel",
                "stackStatus"
              ],
              fields: [

                "diagnosticSha256",
                "logicalResourceId",
                "reasonCategory",
                "resourceStatus",
                "resourceType",
                "schemaVersion",
                "stackLabel",
                "stackStatus"
              ],
              managedCommandOutput: "suppressed",
              maxInputBytes: 1048576,
              maxRecentEvents: 100,
              deprioritizedReasonCategories: ["dependency-failure"],
              eventOrder: "cloudformation-newest-first",
              eventSelection: "newest-non-dependency-failed-else-newest-failed",
              postCredentialDigestRecomputed: true,
              rawReasonHashing: false,
              rawReasonRetention: false,
              recursiveInventory: "exact-two-root-regular-files",
              schemaVersion: "archon.aws-foundation-cfn-failure/v1",
              stackIdentity: "allowlisted-label-only",
              unknownReasonCategoryEligible: true
            }
          ' contracts/aws-foundation-v1.json >/dev/null
          jq -e \
            --slurpfile migration \
              contracts/aws-foundation-policy-migration-v1.json \
            contracts/aws-foundation-core-ami-policy-migration-v1.json \
            contracts/aws-foundation-cloud-runtime-publisher-policy-migration-v1.json \
            .github/workflows/aws-foundation-cloud-runtime-publisher-policy-migration.yml \
            .github/workflows/aws-foundation-cloud-runtime-publisher-policy-migration-driver.yml \
            .github/workflows/aws-foundation-cloud-runtime-publisher-policy-migration-cleanup.yml \
            .github/workflows/datahub-cloud-runtime-image.yml \
            .github/workflows/aws-foundation-core-ami-policy-migration.yml \
            .github/workflows/aws-foundation-core-ami-policy-migration-driver.yml \
            .github/workflows/aws-foundation-core-ami-policy-migration-cleanup.yml \
            --from-file scripts/validate-aws-foundation-policy.jq \
            infra/aws/foundation/github-actions-foundation-policy.json \
            >/dev/null
          test "$(grep -cF \
            '                  - cloudformation:DetectStackResourceDrift' \
            infra/aws/foundation/github-actions-deploy-role.yml)" = "2"
          test "$(grep -cF \
            '                  - cloudformation:BatchDescribeTypeConfigurations' \
            infra/aws/foundation/github-actions-deploy-role.yml)" = "2"
          for sid in \
            DetectExactStageIamFoundationDrift \
            ReadStageIamDriftDetection \
            ReadAndDetectExactProductionStacks \
            ReadStackDriftDetectionStatus; do
            grep -Fq -- "- Sid: ${sid}" \
              infra/aws/foundation/github-actions-deploy-role.yml
          done

      - name: Use the locked Node toolchain
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: infra/aws/package-lock.json

      - name: Install and verify locked AWS CDK dependencies
        run: |
          set -euo pipefail
          contract_node_version="$(
            jq -er '.aws.toolchain.nodeVersion' \
              contracts/aws-foundation-v1.json
          )"
          contract_npm_version="$(
            jq -er '.aws.toolchain.npmVersion' \
              contracts/aws-foundation-v1.json
          )"
          test "${NODE_VERSION}" = "${contract_node_version}"
          test "${NPM_VERSION}" = "${contract_npm_version}"
          test "$(node --version)" = "v${contract_node_version}"
          test "$(npm --version)" = "${contract_npm_version}"
          npm ci --prefix infra/aws --ignore-scripts
          locked_cdk_version="$(
            jq -er '
              .packages["node_modules/aws-cdk"].version |
              select(
                type == "string" and
                test("^[0-9]+\\.[0-9]+\\.[0-9]+$")
              )
            ' infra/aws/package-lock.json
          )"
          installed_cdk_version="$(
            node -p \
              "require('./infra/aws/node_modules/aws-cdk/package.json').version"
          )"
          test "${installed_cdk_version}" = "${locked_cdk_version}"
          printf 'Verified locked AWS CDK CLI %s\n' "${locked_cdk_version}"
          node scripts/verify-exact-npm-overrides.mjs infra/aws
          bash scripts/patch-cdk-brace-expansion.sh infra/aws
          bash scripts/verify-cdk-npm-audit-compensation.sh infra/aws

      - name: Seal the pinned modern bootstrap template
        id: bootstrap_template
        run: bash scripts/seal-cdk-bootstrap-templates.sh

      - name: Render the inline-safe IAM foundation template
        id: iam_foundation_template
        run: |
          set -euo pipefail
          output="${RUNNER_TEMP}/archon-cdk-execution-policy.yaml"
          canonical="${RUNNER_TEMP}/archon-cdk-execution-policy.canonical.json"
          bash scripts/render-inline-cloudformation-template.sh \
            infra/aws/foundation/cdk-execution-policy.yml \
            "${output}" \
            "${canonical}"
          yq_bin="${RUNNER_TEMP}/archon-inline-cfn-yq"
          test -f "${yq_bin}"
          test ! -L "${yq_bin}"
          test -x "${yq_bin}"
          output_sha="$(sha256sum "${output}" | awk '{print $1}')"
          canonical_sha="$(sha256sum "${canonical}" | awk '{print $1}')"
          yq_sha="$(sha256sum "${yq_bin}" | awk '{print $1}')"
          {
            echo "path=${output}"
            echo "sha=${output_sha}"
            echo "canonical_path=${canonical}"
            echo "canonical_sha=${canonical_sha}"
            echo "yq_path=${yq_bin}"
            echo "yq_sha=${yq_sha}"
          } >>"${GITHUB_OUTPUT}"

      - name: Prove exact synthesized runtime action equality for both stages
        run: |
          set -euo pipefail
          for stage in staging production; do
            assembly="${RUNNER_TEMP}/foundation-cdk.out/${stage}"
            (
              cd infra/aws
              ./node_modules/.bin/cdk synth \
                --no-lookups \
                --output "${assembly}" \
                --context "stage=${stage}" \
                --no-notices
            )
            node scripts/verify-aws-runtime-boundary.mjs \
              --contract contracts/aws-foundation-v1.json \
              --stage "${stage}" \
              --templates "${assembly}"
          done

      - name: Revalidate immutable master immediately before AWS trust
        env:
          GH_TOKEN: ${{ github.token }}
          CONTROL_PLANE_SHA: ${{ github.sha }}
          BOOTSTRAP_TEMPLATE: ${{ steps.bootstrap_template.outputs.path }}
          BOOTSTRAP_TEMPLATE_SHA: ${{ steps.bootstrap_template.outputs.sha }}
          IAM_FOUNDATION_TEMPLATE: ${{ steps.iam_foundation_template.outputs.path }}
          IAM_FOUNDATION_TEMPLATE_SHA: ${{ steps.iam_foundation_template.outputs.sha }}
          IAM_FOUNDATION_CANONICAL_JSON: ${{ steps.iam_foundation_template.outputs.canonical_path }}
          IAM_FOUNDATION_SEMANTIC_SHA: ${{ steps.iam_foundation_template.outputs.canonical_sha }}
          IAM_FOUNDATION_YQ_BIN: ${{ steps.iam_foundation_template.outputs.yq_path }}
          IAM_FOUNDATION_YQ_SHA: ${{ steps.iam_foundation_template.outputs.yq_sha }}
          STAGING_PRIMARY_BOOTSTRAP_TEMPLATE: ${{ steps.bootstrap_template.outputs.staging_primary_path }}
          STAGING_PRIMARY_BOOTSTRAP_TEMPLATE_SHA: ${{ steps.bootstrap_template.outputs.staging_primary_sha }}
          STAGING_EDGE_BOOTSTRAP_TEMPLATE: ${{ steps.bootstrap_template.outputs.staging_edge_path }}
          STAGING_EDGE_BOOTSTRAP_TEMPLATE_SHA: ${{ steps.bootstrap_template.outputs.staging_edge_sha }}
          PRODUCTION_PRIMARY_BOOTSTRAP_TEMPLATE: ${{ steps.bootstrap_template.outputs.production_primary_path }}
          PRODUCTION_PRIMARY_BOOTSTRAP_TEMPLATE_SHA: ${{ steps.bootstrap_template.outputs.production_primary_sha }}
          PRODUCTION_EDGE_BOOTSTRAP_TEMPLATE: ${{ steps.bootstrap_template.outputs.production_edge_path }}
          PRODUCTION_EDGE_BOOTSTRAP_TEMPLATE_SHA: ${{ steps.bootstrap_template.outputs.production_edge_sha }}
        run: |
          set -euo pipefail
          test "$(
            gh api \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2026-03-10" \
              "/repos/${GITHUB_REPOSITORY}/git/ref/heads/${EXPECTED_BRANCH}" |
              jq -er '.object.sha'
          )" = "${CONTROL_PLANE_SHA}"
          test "$(git rev-parse HEAD)" = "${CONTROL_PLANE_SHA}"
          test "$(sha256sum "${BOOTSTRAP_TEMPLATE}" | awk '{print $1}')" = \
            "${BOOTSTRAP_TEMPLATE_SHA}"
          test "$(sha256sum "${IAM_FOUNDATION_TEMPLATE}" | awk '{print $1}')" = \
            "${IAM_FOUNDATION_TEMPLATE_SHA}"
          test "$(wc -c <"${IAM_FOUNDATION_TEMPLATE}" | tr -d '[:space:]')" \
            -le 51200
          test "$(sha256sum "${IAM_FOUNDATION_CANONICAL_JSON}" | awk '{print $1}')" = \
            "${IAM_FOUNDATION_SEMANTIC_SHA}"
          jq -e '
            type == "object" and
            (.Resources | type == "object" and length > 0)
          ' "${IAM_FOUNDATION_CANONICAL_JSON}" >/dev/null
          test -f "${IAM_FOUNDATION_YQ_BIN}"
          test ! -L "${IAM_FOUNDATION_YQ_BIN}"
          test -x "${IAM_FOUNDATION_YQ_BIN}"
          test "${IAM_FOUNDATION_YQ_SHA}" = "$(
            jq -er \
              '.aws.inlineTemplateRendering.yamlParser.linuxAmd64Sha256' \
              contracts/aws-foundation-v1.json
          )"
          test "$(sha256sum "${IAM_FOUNDATION_YQ_BIN}" | awk '{print $1}')" = \
            "${IAM_FOUNDATION_YQ_SHA}"
          for tuple in \
            "STAGING_PRIMARY_BOOTSTRAP_TEMPLATE STAGING_PRIMARY_BOOTSTRAP_TEMPLATE_SHA" \
            "STAGING_EDGE_BOOTSTRAP_TEMPLATE STAGING_EDGE_BOOTSTRAP_TEMPLATE_SHA" \
            "PRODUCTION_PRIMARY_BOOTSTRAP_TEMPLATE PRODUCTION_PRIMARY_BOOTSTRAP_TEMPLATE_SHA" \
            "PRODUCTION_EDGE_BOOTSTRAP_TEMPLATE PRODUCTION_EDGE_BOOTSTRAP_TEMPLATE_SHA"; do
            read -r path_name sha_name <<<"${tuple}"
            test "$(sha256sum "${!path_name}" | awk '{print $1}')" = \
              "${!sha_name}"
          done
          test -z "$(git status --porcelain --untracked-files=all)"

      - name: Configure short-lived foundation credentials through OIDC
        uses: aws-actions/configure-aws-credentials@e6de054238d6b7531b4efff3b6587d9aade6a06c # v6.2.3
        with:
          role-to-assume: ${{ vars.AWS_FOUNDATION_ROLE_ARN }}
          role-session-name: archon-foundation-${{ github.run_id }}-${{ github.run_attempt }}
          role-duration-seconds: 3600
          aws-region: ${{ vars.AWS_REGION }}
          allowed-account-ids: ${{ vars.AWS_ACCOUNT_ID }}
          mask-aws-account-id: true
          unset-current-credentials: true

      - name: Verify exact foundation identity and resolved-only inline policy
        id: identity
        env:
          EXPECTED_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}
          EXPECTED_REGION: ${{ vars.AWS_REGION }}
          EXPECTED_ROLE_ARN: ${{ vars.AWS_FOUNDATION_ROLE_ARN }}
          EXPECTED_SESSION_NAME: archon-foundation-${{ github.run_id }}-${{ github.run_attempt }}
        run: |
          set -euo pipefail
          echo "::add-mask::${EXPECTED_ACCOUNT_ID}"
          echo "::add-mask::${EXPECTED_ROLE_ARN}"
          identity="${RUNNER_TEMP}/foundation-identity.json"
          aws sts get-caller-identity --output json >"${identity}"
          jq -e \
            --arg account "${EXPECTED_ACCOUNT_ID}" \
            --arg session "${EXPECTED_SESSION_NAME}" '
              .Account == $account and
              .Arn == (
                "arn:aws:sts::" + $account +
                ":assumed-role/archon-datahub-github-foundation/" + $session
              )
            ' "${identity}" >/dev/null
          test "${AWS_REGION}" = "${EXPECTED_REGION}"
          test "${AWS_DEFAULT_REGION}" = "${EXPECTED_REGION}"

          role_json="${RUNNER_TEMP}/foundation-role.json"
          aws iam get-role \
            --role-name "${FOUNDATION_ROLE_NAME}" \
            --output json >"${role_json}"
          jq -e \
            --arg account "${EXPECTED_ACCOUNT_ID}" \
            --arg subject \
              "repo:upgradedev/archon-datahub:environment:aws-foundation" '
              .Role.RoleName == "archon-datahub-github-foundation" and
              .Role.MaxSessionDuration == 3600 and
              (.Role.PermissionsBoundary == null) and
              (.Role.AssumeRolePolicyDocument.Statement | length) == 1 and
              .Role.AssumeRolePolicyDocument.Statement[0].Action ==
                "sts:AssumeRoleWithWebIdentity" and
              .Role.AssumeRolePolicyDocument.Statement[0].Principal.Federated ==
                ("arn:aws:iam::" + $account +
                  ":oidc-provider/token.actions.githubusercontent.com") and
              .Role.AssumeRolePolicyDocument.Statement[0].Condition.StringEquals[
                "token.actions.githubusercontent.com:aud"
              ] == "sts.amazonaws.com" and
              .Role.AssumeRolePolicyDocument.Statement[0].Condition.StringEquals[
                "token.actions.githubusercontent.com:sub"
              ] == $subject
            ' "${role_json}" >/dev/null
          control_policy_arn="$(
            printf 'arn:aws:iam::%s:policy/archon-aws-foundation-control' \
              "${EXPECTED_ACCOUNT_ID}"
          )"
          assets_policy_arn="$(
            printf 'arn:aws:iam::%s:policy/archon-aws-foundation-assets' \
              "${EXPECTED_ACCOUNT_ID}"
          )"
          identity_policy_arn="$(
            printf 'arn:aws:iam::%s:policy/archon-aws-foundation-identity' \
              "${EXPECTED_ACCOUNT_ID}"
          )"
          attachments_policy_arn="$(
            printf 'arn:aws:iam::%s:policy/archon-aws-foundation-attachments' \
              "${EXPECTED_ACCOUNT_ID}"
          )"
          aws iam list-attached-role-policies \
            --role-name "${FOUNDATION_ROLE_NAME}" \
            --output json |
            jq -e \
              --arg control "${control_policy_arn}" \
              --arg assets "${assets_policy_arn}" \
              --arg identity "${identity_policy_arn}" \
              --arg attachments "${attachments_policy_arn}" '
                (.AttachedPolicies | length) == 4 and
                (10 - (.AttachedPolicies | length)) >= 6 and
                (.AttachedPolicies | sort_by(.PolicyArn)) ==
                  ([
                    {
                      PolicyArn: $control,
                      PolicyName: "archon-aws-foundation-control"
                    },
                    {
                      PolicyArn: $assets,
                      PolicyName: "archon-aws-foundation-assets"
                    },
                    {
                      PolicyArn: $identity,
                      PolicyName: "archon-aws-foundation-identity"
                    },
                    {
                      PolicyArn: $attachments,
                      PolicyName: "archon-aws-foundation-attachments"
                    }
                  ] | sort_by(.PolicyArn))
              ' >/dev/null
          aws iam list-role-policies \
            --role-name "${FOUNDATION_ROLE_NAME}" \
            --output json |
            jq -e '.PolicyNames == []' >/dev/null

          canonical='
            def sorted_array:
              if type == "array" then sort else [.] end;
            .Statement |= (
              map(
                .Action |= sorted_array |
                .Resource |= sorted_array
              ) |
              sort_by(.Sid)
            )
          '
          control_rendered="${RUNNER_TEMP}/foundation-control-resolved.json"
          assets_rendered="${RUNNER_TEMP}/foundation-assets-resolved.json"
          identity_rendered="${RUNNER_TEMP}/foundation-identity-resolved.json"
          attachments_rendered="${RUNNER_TEMP}/foundation-attachments-resolved.json"
          node scripts/render-aws-foundation-policy.mjs \
            --input infra/aws/foundation/github-actions-foundation-policy.json \
            --control-output "${control_rendered}" \
            --assets-output "${assets_rendered}" \
            --identity-output "${identity_rendered}" \
            --attachments-output "${attachments_rendered}" \
            --account "${EXPECTED_ACCOUNT_ID}"
          actual_bundle="${RUNNER_TEMP}/foundation-policy-actual.jsonl"
          : >"${actual_bundle}"
          for group in control assets identity attachments; do
            case "${group}" in
              control)
                policy_arn="${control_policy_arn}"
                rendered="${control_rendered}"
                ;;
              assets)
                policy_arn="${assets_policy_arn}"
                rendered="${assets_rendered}"
                ;;
              identity)
                policy_arn="${identity_policy_arn}"
                rendered="${identity_rendered}"
                ;;
              attachments)
                policy_arn="${attachments_policy_arn}"
                rendered="${attachments_rendered}"
                ;;
            esac
            metadata="${RUNNER_TEMP}/foundation-${group}-metadata.json"
            deployed="${RUNNER_TEMP}/foundation-${group}-deployed.json"
            expected="${RUNNER_TEMP}/foundation-${group}-expected.json"
            actual="${RUNNER_TEMP}/foundation-${group}-actual.json"
            aws iam get-policy \
              --policy-arn "${policy_arn}" \
              --output json >"${metadata}"
            aws iam get-policy-version \
              --policy-arn "${policy_arn}" \
              --version-id "$(
                jq -er '.Policy.DefaultVersionId' "${metadata}"
              )" \
              --output json >"${deployed}"
            jq -S "${canonical}" "${rendered}" >"${expected}"
            jq '.PolicyVersion.Document' "${deployed}" |
              jq -S "${canonical}" >"${actual}"
            cmp -s "${expected}" "${actual}"
            compact_size="$(
              jq -c '.PolicyVersion.Document' "${deployed}" |
                wc -c |
                awk '{print $1 - 1}'
            )"
            test "${compact_size}" -le 6144
            jq -cS "${canonical}" "${actual}" >>"${actual_bundle}"
          done
          actual_sha="$(sha256sum "${actual_bundle}" | awk '{print $1}')"
          echo "actual_sha=${actual_sha}" >>"${GITHUB_OUTPUT}"

      - name: Reconcile, drift-check, and seal the AWS foundation
        id: reconcile
        env:
          EXPECTED_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}
          AWS_SHARED_API_GATEWAY_ROLE_ARN_SHA256: ${{ vars.AWS_SHARED_API_GATEWAY_ROLE_ARN_SHA256 }}
          CONTROL_PLANE_SHA: ${{ github.sha }}
          GITHUB_WORKFLOW_SHA: ${{ github.workflow_sha }}
          GH_TOKEN: ${{ github.token }}
          BOOTSTRAP_TEMPLATE: ${{ steps.bootstrap_template.outputs.path }}
          BOOTSTRAP_TEMPLATE_SHA: ${{ steps.bootstrap_template.outputs.sha }}
          IAM_FOUNDATION_TEMPLATE: ${{ steps.iam_foundation_template.outputs.path }}
          IAM_FOUNDATION_TEMPLATE_SHA: ${{ steps.iam_foundation_template.outputs.sha }}
          IAM_FOUNDATION_CANONICAL_JSON: ${{ steps.iam_foundation_template.outputs.canonical_path }}
          IAM_FOUNDATION_SEMANTIC_SHA: ${{ steps.iam_foundation_template.outputs.canonical_sha }}
          IAM_FOUNDATION_YQ_BIN: ${{ steps.iam_foundation_template.outputs.yq_path }}
          IAM_FOUNDATION_YQ_SHA: ${{ steps.iam_foundation_template.outputs.yq_sha }}
          STAGING_PRIMARY_BOOTSTRAP_TEMPLATE: ${{ steps.bootstrap_template.outputs.staging_primary_path }}
          STAGING_PRIMARY_BOOTSTRAP_TEMPLATE_SHA: ${{ steps.bootstrap_template.outputs.staging_primary_sha }}
          STAGING_EDGE_BOOTSTRAP_TEMPLATE: ${{ steps.bootstrap_template.outputs.staging_edge_path }}
          STAGING_EDGE_BOOTSTRAP_TEMPLATE_SHA: ${{ steps.bootstrap_template.outputs.staging_edge_sha }}
          PRODUCTION_PRIMARY_BOOTSTRAP_TEMPLATE: ${{ steps.bootstrap_template.outputs.production_primary_path }}
          PRODUCTION_PRIMARY_BOOTSTRAP_TEMPLATE_SHA: ${{ steps.bootstrap_template.outputs.production_primary_sha }}
          PRODUCTION_EDGE_BOOTSTRAP_TEMPLATE: ${{ steps.bootstrap_template.outputs.production_edge_path }}
          PRODUCTION_EDGE_BOOTSTRAP_TEMPLATE_SHA: ${{ steps.bootstrap_template.outputs.production_edge_sha }}
          PINNED_BOOTSTRAP_VERSION: ${{ steps.bootstrap_template.outputs.version }}
          FOUNDATION_POLICY_ACTUAL_SHA: ${{ steps.identity.outputs.actual_sha }}
          STAGING_CLOUDFRONT_DOMAIN_NAME: ${{ vars.STAGING_CLOUDFRONT_DOMAIN_NAME }}
          STAGING_CLOUDFRONT_HOSTED_ZONE_ID: ${{ vars.STAGING_CLOUDFRONT_HOSTED_ZONE_ID }}
          PRODUCTION_CLOUDFRONT_DOMAIN_NAME: ${{ vars.PRODUCTION_CLOUDFRONT_DOMAIN_NAME }}
          PRODUCTION_CLOUDFRONT_HOSTED_ZONE_ID: ${{ vars.PRODUCTION_CLOUDFRONT_HOSTED_ZONE_ID }}
        run: bash scripts/reconcile-aws-foundation.sh

      - name: Reconcile, drift-check, and seal Core AMI builder identities
        id: core_ami_foundation
        env:
          EXPECTED_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}
          CONTROL_PLANE_SHA: ${{ github.sha }}
          GH_TOKEN: ${{ github.token }}
        run: bash scripts/reconcile-datahub-core-ami-foundation.sh

      - name: Revalidate master before credential clearing
        env:
          CONTROL_PLANE_SHA: ${{ github.sha }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$(
            gh api \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2026-03-10" \
              "/repos/${GITHUB_REPOSITORY}/git/ref/heads/${EXPECTED_BRANCH}" |
              jq -er '.object.sha'
          )" = "${CONTROL_PLANE_SHA}"
          test "$(git rev-parse HEAD)" = "${CONTROL_PLANE_SHA}"
          test -z "$(git status --porcelain --untracked-files=all)"

      - name: Clear AWS credentials before artifact handling
        id: clear_aws_credentials
        if: ${{ always() }}
        run: |
          set -euo pipefail
          {
            echo "AWS_ACCESS_KEY_ID="
            echo "AWS_SECRET_ACCESS_KEY="
            echo "AWS_SESSION_TOKEN="
            echo "AWS_SECURITY_TOKEN="
          } >>"${GITHUB_ENV}"
          echo "cleared=true" >>"${GITHUB_OUTPUT}"

      - name: Validate checksum-sealed sanitized failure evidence
        id: failure_evidence
        if: ${{ failure() && steps.clear_aws_credentials.outputs.cleared == 'true' }}
        env:
          FAILURE_EVIDENCE_DIR: ${{ runner.temp }}/aws-foundation-failure
        run: |
          set -euo pipefail
          if [[ -n "${AWS_ACCESS_KEY_ID:-}" || \
            -n "${AWS_SECRET_ACCESS_KEY:-}" || \
            -n "${AWS_SESSION_TOKEN:-}" || \
            -n "${AWS_SECURITY_TOKEN:-}" ]]; then
            echo "::error::AWS credentials remained available during failure evidence validation" >&2
            exit 1
          fi
          if [[ ! -e "${FAILURE_EVIDENCE_DIR}" && ! -L "${FAILURE_EVIDENCE_DIR}" ]]; then
            echo "available=false" >>"${GITHUB_OUTPUT}"
            exit 0
          fi
          test -d "${FAILURE_EVIDENCE_DIR}"
          test ! -L "${FAILURE_EVIDENCE_DIR}"
          if ! actual_inventory_sha="$(
            find -P "${FAILURE_EVIDENCE_DIR}" \
              -mindepth 1 -printf '%P\t%y\0' |
              LC_ALL=C sort -z |
              sha256sum |
              awk '{print $1}'
          )"; then
            echo "::error::Unable to inventory sanitized failure evidence" >&2
            exit 1
          fi
          if ! expected_inventory_sha="$(
            printf 'SHA256SUMS\tf\0cfn-failure.json\tf\0' |
              sha256sum |
              awk '{print $1}'
          )"; then
            echo "::error::Unable to derive the expected failure evidence inventory" >&2
            exit 1
          fi
          if [[ ! "${actual_inventory_sha}" =~ ^[0-9a-f]{64}$ || \
            "${actual_inventory_sha}" != "${expected_inventory_sha}" ]]; then
            echo "::error::Sanitized failure evidence inventory mismatch" >&2
            exit 1
          fi
          for evidence_path in cfn-failure.json SHA256SUMS; do
            test -f "${FAILURE_EVIDENCE_DIR}/${evidence_path}"
            test ! -L "${FAILURE_EVIDENCE_DIR}/${evidence_path}"
          done
          if ! (
            cd "${FAILURE_EVIDENCE_DIR}" &&
              test "$(wc -l <SHA256SUMS)" -eq 1 &&
              grep -Eq '^[0-9a-f]{64}  cfn-failure.json$' SHA256SUMS &&
              sha256sum --check --strict SHA256SUMS >/dev/null 2>&1
          ); then
            echo "::error::Sanitized failure evidence checksum mismatch" >&2
            exit 1
          fi
          if ! test "$(wc -c <"${FAILURE_EVIDENCE_DIR}/cfn-failure.json")" -le 2048; then
            echo "::error::Sanitized failure evidence exceeded its size limit" >&2
            exit 1
          fi
          if ! canonical_diagnostic="$(
            mktemp "${RUNNER_TEMP}/aws-foundation-failure-canonical.XXXXXX"
          )"; then
            echo "::error::Unable to stage canonical failure evidence validation" >&2
            exit 1
          fi
          trap 'rm -f -- "${canonical_diagnostic}"' EXIT
          if ! jq -cS . \
            "${FAILURE_EVIDENCE_DIR}/cfn-failure.json" \
            >"${canonical_diagnostic}" 2>/dev/null; then
            echo "::error::Sanitized failure evidence was not valid JSON" >&2
            exit 1
          fi
          if ! cmp -s \
            "${FAILURE_EVIDENCE_DIR}/cfn-failure.json" \
            "${canonical_diagnostic}"; then
            echo "::error::Sanitized failure evidence was not canonical JSON" >&2
            exit 1
          fi
          if ! jq -e '
            (keys | sort) == [
              "diagnosticSha256",
              "logicalResourceId",
              "reasonCategory",
              "resourceStatus",
              "resourceType",
              "schemaVersion",
              "stackLabel",
              "stackStatus"
            ] and
            .schemaVersion == "archon.aws-foundation-cfn-failure/v1" and
            (.stackLabel | IN(
              "governed-canary-roles",
              "production-bootstrap-edge",
              "production-bootstrap-primary",
              "production-deploy",
              "production-iam",
              "shared-api-gateway",
              "staging-bootstrap-edge",
              "staging-bootstrap-primary",
              "staging-deploy",
              "staging-iam"
            )) and
            (.stackStatus | IN(
              "CREATE_COMPLETE", "CREATE_FAILED", "CREATE_IN_PROGRESS",
              "DELETE_COMPLETE", "DELETE_FAILED", "DELETE_IN_PROGRESS",
              "IMPORT_COMPLETE", "IMPORT_IN_PROGRESS",
              "IMPORT_ROLLBACK_COMPLETE", "IMPORT_ROLLBACK_FAILED",
              "IMPORT_ROLLBACK_IN_PROGRESS", "REVIEW_IN_PROGRESS",
              "ROLLBACK_COMPLETE", "ROLLBACK_FAILED", "ROLLBACK_IN_PROGRESS",
              "UNKNOWN", "UPDATE_COMPLETE",
              "UPDATE_COMPLETE_CLEANUP_IN_PROGRESS", "UPDATE_FAILED",
              "UPDATE_IN_PROGRESS", "UPDATE_ROLLBACK_COMPLETE",
              "UPDATE_ROLLBACK_COMPLETE_CLEANUP_IN_PROGRESS",
              "UPDATE_ROLLBACK_FAILED", "UPDATE_ROLLBACK_IN_PROGRESS"
            )) and
            (.logicalResourceId | test("^[A-Za-z][A-Za-z0-9]{0,254}$")) and
            (.resourceType | test("^(AWS::[A-Za-z0-9]+::[A-Za-z0-9]+|Custom::[A-Za-z0-9._-]+)$")) and
            (.resourceStatus | IN(
              "CREATE_FAILED", "DELETE_FAILED", "IMPORT_ROLLBACK_FAILED",
              "ROLLBACK_FAILED", "UPDATE_FAILED", "UPDATE_ROLLBACK_FAILED"
            )) and
            (.reasonCategory | IN(
              "access-denied", "already-exists", "dependency-failure",
              "invalid-request", "quota-exceeded", "resource-conflict",
              "service-failure", "timeout", "unknown"
            )) and
            (.diagnosticSha256 | test("^[0-9a-f]{64}$"))
          ' "${FAILURE_EVIDENCE_DIR}/cfn-failure.json" >/dev/null 2>&1; then
            echo "::error::Sanitized failure evidence failed schema validation" >&2
            exit 1
          fi
          if ! canonical_safe_json="$(
            jq -cS '{
              logicalResourceId,
              reasonCategory,
              resourceStatus,
              resourceType,
              schemaVersion,
              stackLabel,
              stackStatus
            }' "${FAILURE_EVIDENCE_DIR}/cfn-failure.json" 2>/dev/null
          )"; then
            echo "::error::Unable to canonicalize sanitized diagnostic fields" >&2
            exit 1
          fi
          if ! computed_digest="$(
            printf '%s' "${canonical_safe_json}" |
              sha256sum |
              awk '{print $1}'
          )"; then
            echo "::error::Unable to recompute sanitized diagnostic digest" >&2
            exit 1
          fi
          if [[ ! "${computed_digest}" =~ ^[0-9a-f]{64}$ ]] || \
            ! jq -e --arg computedDigest "${computed_digest}" \
              '.diagnosticSha256 == $computedDigest' \
              "${FAILURE_EVIDENCE_DIR}/cfn-failure.json" \
              >/dev/null 2>&1; then
            echo "::error::Sanitized diagnostic digest mismatch" >&2
            exit 1
          fi
          if ! rm -f -- "${canonical_diagnostic}"; then
            echo "::error::Unable to remove canonical validation staging" >&2
            exit 1
          fi
          trap - EXIT
          echo "available=true" >>"${GITHUB_OUTPUT}"

      - name: Retain checksum-sealed sanitized foundation failure evidence
        if: ${{ failure() && steps.clear_aws_credentials.outputs.cleared == 'true' && steps.failure_evidence.outputs.available == 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: aws-foundation-failure-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            ${{ runner.temp }}/aws-foundation-failure/SHA256SUMS
            ${{ runner.temp }}/aws-foundation-failure/cfn-failure.json
          if-no-files-found: error
          retention-days: 90
          compression-level: 0
          include-hidden-files: false

      - name: Reverify sanitized Core AMI foundation evidence
        env:
          EVIDENCE_DIR: ${{ steps.core_ami_foundation.outputs.path }}
          EXPECTED_EVIDENCE_SHA: ${{ steps.core_ami_foundation.outputs.evidence_sha }}
        run: |
          set -euo pipefail
          for variable in \
            AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY AWS_SESSION_TOKEN \
            AWS_SECURITY_TOKEN; do
            test -z "${!variable:-}"
          done
          test -d "${EVIDENCE_DIR}"
          test ! -L "${EVIDENCE_DIR}"
          expected="$({
            printf '%s\n' \
              SHA256SUMS attestation-predicate.json \
              core-ami-foundation.json
          } | sort)"
          actual="$(
            find "${EVIDENCE_DIR}" -mindepth 1 -maxdepth 1 -type f \
              -printf '%f\n' | sort
          )"
          test "${actual}" = "${expected}"
          for file in \
            SHA256SUMS attestation-predicate.json \
            core-ami-foundation.json; do
            test -f "${EVIDENCE_DIR}/${file}"
            test ! -L "${EVIDENCE_DIR}/${file}"
            test "$(stat -c '%s' "${EVIDENCE_DIR}/${file}")" -le 16384
          done
          (
            cd "${EVIDENCE_DIR}"
            sha256sum --check --strict SHA256SUMS
          )
          test "$(
            sha256sum "${EVIDENCE_DIR}/core-ami-foundation.json" |
              awk '{print $1}'
          )" = "${EXPECTED_EVIDENCE_SHA}"
          jq -e '
            .schemaVersion ==
              "archon.aws-core-ami-foundation-evidence/v1" and
            .source.repository == "upgradedev/archon-datahub" and
            .source.ref == "refs/heads/master" and
            .stack.name == "Archon-DataHub-Core-AMI-Builder-Foundation" and
            .stack.region == "eu-west-1" and
            .stack.terminationProtection == true and
            .stack.drift.status == "IN_SYNC" and
            .stack.drift.coverage ==
              "cloudformation-supported-resources" and
            .stack.drift.globalTimeoutSeconds == 900 and
            .identities.githubBuildRole.maxSessionDurationSeconds == 7200 and
            .identities.githubBuildRole.requestTagObservation == {
              action: "cloudtrail:LookupEvents",
              region: "eu-west-1",
              requiredTags: {
                Application: "archon-datahub",
                Environment: "staging",
                ManagedBy: "github-actions",
                "archon:BuildRun": "exact-build-id",
                "archon:Purpose": "datahub-core-ami"
              },
              resourceScope: "wildcard-required-by-aws-action"
            } and
            .identities.ec2Builder.managedPolicy ==
              "arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore" and
            .githubVariables == {
              buildRole: "AWS_CORE_AMI_BUILD_ROLE_ARN",
              instanceProfile: "AWS_CORE_AMI_BUILDER_INSTANCE_PROFILE"
            } and
            .idleAwsCost == "zero" and
            .validation == "passed"
          ' "${EVIDENCE_DIR}/core-ami-foundation.json" >/dev/null

      - name: Attest exact sanitized Core AMI foundation evidence
        uses: actions/attest@f7c74d28b9d84cb8768d0b8ca14a4bac6ef463e6 # v4.2.0
        with:
          subject-checksums: ${{ steps.core_ami_foundation.outputs.subject }}
          predicate-type: https://github.com/upgradedev/archon-datahub/attestations/aws-core-ami-foundation/v1
          predicate-path: ${{ steps.core_ami_foundation.outputs.predicate }}

      - name: Retain sanitized Core AMI foundation evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: aws-core-ami-foundation-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ steps.core_ami_foundation.outputs.path }}
          if-no-files-found: error
          retention-days: 90
          compression-level: 0
          include-hidden-files: false

      - name: Reverify sanitized foundation evidence
        env:
          CONTROL_PLANE_SHA: ${{ github.sha }}
          EVIDENCE_DIR: ${{ steps.reconcile.outputs.path }}
          EXPECTED_FOUNDATION_SHA: ${{ steps.reconcile.outputs.foundation_sha }}
          EXPECTED_MANIFEST_SHA: ${{ steps.reconcile.outputs.manifest_sha }}
          EXPECTED_DRIFT_SHA: ${{ steps.reconcile.outputs.drift_sha }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$(
            gh api \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2026-03-10" \
              "/repos/${GITHUB_REPOSITORY}/git/ref/heads/${EXPECTED_BRANCH}" |
              jq -er '.object.sha'
          )" = "${CONTROL_PLANE_SHA}"
          test -d "${EVIDENCE_DIR}"
          test ! -L "${EVIDENCE_DIR}"
          test "$(
            sha256sum "${EVIDENCE_DIR}/foundation.json" | awk '{print $1}'
          )" = "${EXPECTED_FOUNDATION_SHA}"
          test "$(
            sha256sum "${EVIDENCE_DIR}/manifest.json" | awk '{print $1}'
          )" = "${EXPECTED_MANIFEST_SHA}"
          test "$(
            sha256sum "${EVIDENCE_DIR}/drift.json" | awk '{print $1}'
          )" = "${EXPECTED_DRIFT_SHA}"
          (
            cd "${EVIDENCE_DIR}"
            sha256sum --check --strict SHA256SUMS
          )

      - name: Attest exact sanitized foundation evidence
        uses: actions/attest@f7c74d28b9d84cb8768d0b8ca14a4bac6ef463e6 # v4.2.0
        with:
          subject-checksums: ${{ steps.reconcile.outputs.subject }}
          predicate-type: https://github.com/upgradedev/archon-datahub/attestations/aws-foundation/v1
          predicate-path: ${{ steps.reconcile.outputs.predicate }}

      - name: Retain sanitized foundation evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: aws-foundation-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ steps.reconcile.outputs.path }}
          if-no-files-found: error
          retention-days: 90
          compression-level: 0
          include-hidden-files: false

      - name: Publish non-sensitive handoff
        env:
          FOUNDATION_SHA: ${{ steps.reconcile.outputs.foundation_sha }}
          DRIFT_SHA: ${{ steps.reconcile.outputs.drift_sha }}
          DEPLOY_ROLE_BINDING_SHA: ${{ steps.reconcile.outputs.deploy_role_binding_sha }}
          CANARY_ROLE_BINDING_SHA: ${{ steps.reconcile.outputs.canary_role_binding_sha }}
          OPERATIONAL_ROLE_BINDING_SHA: ${{ steps.reconcile.outputs.operational_role_binding_sha }}
          CLOUD_RUNTIME_PUBLISHER_ROLE_ARN: ${{ steps.reconcile.outputs.cloud_runtime_publisher_role_arn }}
          CLOUD_RUNTIME_PUBLISHER_BINDING_SHA: ${{ steps.reconcile.outputs.cloud_runtime_publisher_binding_sha }}
          DATAHUB_CLOUD_TRIAL_STAGING_ROLE_ARN: ${{ steps.reconcile.outputs.datahub_cloud_trial_staging_role_arn }}
          DATAHUB_CLOUD_TRIAL_PRODUCTION_ROLE_ARN: ${{ steps.reconcile.outputs.datahub_cloud_trial_production_role_arn }}
          APPLICATION_STACK_ROLE_TRANSITION: ${{ steps.reconcile.outputs.application_stack_role_transition }}
          SHARED_API_GATEWAY_MODE: ${{ steps.reconcile.outputs.shared_api_gateway_mode }}
          DRIFT_STACK_COUNT: ${{ steps.reconcile.outputs.drift_stack_count }}
          CORE_AMI_FOUNDATION_SHA: ${{ steps.core_ami_foundation.outputs.evidence_sha }}
          CORE_AMI_BUILD_ROLE_ARN: ${{ steps.core_ami_foundation.outputs.build_role_arn }}
          CORE_AMI_BUILDER_INSTANCE_PROFILE: ${{ steps.core_ami_foundation.outputs.instance_profile }}
        run: |
          set -euo pipefail
          : "${DATAHUB_CLOUD_TRIAL_STAGING_ROLE_ARN:?staging Cloud trial role handoff missing}"
          : "${DATAHUB_CLOUD_TRIAL_PRODUCTION_ROLE_ARN:?production Cloud trial role handoff missing}"
          {
            echo "### AWS foundation reconciled"
            echo
            echo "- Regions: \`eu-west-1\`, \`us-east-1\`"
            echo "- Bootstraps: \`CDKToolkit-archonstg\`, \`CDKToolkit-archonprd\` (version 32)"
            echo "- Stage execution policies and runtime boundaries: exact; CloudFormation-supported resources IN_SYNC"
            echo "- GitHub deploy roles: separate staging and production OIDC subjects"
            echo "- Governed canary roles: prepare, approval, and recovery read paths"
            echo "- Operational roles: stage judge access plus production Cloud runtime publisher, posture, runtime, and paging"
            case "${SHARED_API_GATEWAY_MODE}" in
              foundation-managed)
                test "${DRIFT_STACK_COUNT}" = "10"
                echo "- Shared API Gateway logging account: exact foundation-managed role"
                echo "- Managed stack inventory: \`10\`"
                ;;
              external-pinned)
                test "${DRIFT_STACK_COUNT}" = "9"
                echo "- Shared API Gateway logging account: pinned external binding, unchanged, no takeover"
                echo "- Managed stack inventory: \`9\` plus one external account binding"
                ;;
              *)
                echo "::error::invalid shared API Gateway evidence mode"
                exit 1
                ;;
            esac
            echo "- Application stack RoleARN transition: \`${APPLICATION_STACK_ROLE_TRANSITION}\`"
            echo "- Managed-stack drift digest: \`${DRIFT_SHA}\`"
            echo "- Deploy-role binding digest: \`${DEPLOY_ROLE_BINDING_SHA}\`"
            echo "- Canary-role binding digest: \`${CANARY_ROLE_BINDING_SHA}\`"
            echo "- Operational-role binding digest: \`${OPERATIONAL_ROLE_BINDING_SHA}\`"
            echo "- Set production AWS_CLOUD_RUNTIME_IMAGE_ROLE_ARN to ${CLOUD_RUNTIME_PUBLISHER_ROLE_ARN}"
            echo "- Set staging AWS_DATAHUB_CLOUD_TRIAL_ROLE_ARN to ${DATAHUB_CLOUD_TRIAL_STAGING_ROLE_ARN}"
            echo "- Set production AWS_DATAHUB_CLOUD_TRIAL_ROLE_ARN to ${DATAHUB_CLOUD_TRIAL_PRODUCTION_ROLE_ARN}"
            echo "- Cloud runtime publisher binding digest: \`${CLOUD_RUNTIME_PUBLISHER_BINDING_SHA}\`"
            echo "- Cloud runtime publisher: exact master/workflow OIDC trust; 1800-second requested session; exact tagged ECR repository only"
            echo "- Core AMI builder: stable zero-idle-cost role/profile, independent of runtime KMS/Bedrock permissions"
            echo "- Core AMI build OIDC role max/session request: 7200 seconds"
            echo "- Set staging AWS_CORE_AMI_BUILD_ROLE_ARN to ${CORE_AMI_BUILD_ROLE_ARN}"
            echo "- Set staging AWS_CORE_AMI_BUILDER_INSTANCE_PROFILE to ${CORE_AMI_BUILDER_INSTANCE_PROFILE}"
            echo "- Core AMI foundation evidence digest: ${CORE_AMI_FOUNDATION_SHA}"
            echo "- Foundation evidence digest: ${FOUNDATION_SHA}"
          } >>"${GITHUB_STEP_SUMMARY}"
